{% extends 'base.html.twig' %}

{% block title %}
Panel de Editor
{% endblock %}

{% block body %}

	<h1>Panel del Editor</h1>
	<div class="acciones">
		<a href="{{ path('noticia_nueva') }}" class="btn btn-success">➕ Crear nueva noticia</a>
		<a href="{{ path('categoria_listar') }}" class="btn btn-info">📂 Gestionar categorías</a>
	</div>

	<h2>Listado de noticias</h2>
	<table class="table">
		<thead>
			<tr>
				<th>Título</th>
				<th>Categoría</th>
				<th>Estado</th>
				<th>Fecha</th>
				<th>Acciones</th>
			</tr>
		</thead>
		<tbody>
			{% for noticia in noticias %}
				<tr>
					<td>{{ noticia.titulo }}</td>
					<td>
						ejemplo categoria</td>
					<td>{{ noticia.estado }}</td>
					<td>{{ noticia.fechaPublicacion|date('d/m/Y') }}</td>
					<td>
						<p>✏️ Editar</p>
						<p>🗑️ Eliminar</p>
					</td>
				</tr>
			{% else %}
				<tr>
					<td colspan="5">No hay noticias creadas aún.</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>

	<h2>Estadísticas rápidas</h2>
	<ul>
		<li>Total de noticias:
			{{ noticias|length }}</li>
		<li>Categorías disponibles:
			{{ categorias|length }}</li>
	</ul>
{% endblock %}
