{% extends 'base.html.twig' %}

{% block title %}Panel de Administración - GameNews{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/admin.css') }}">
{% endblock %}

{% block body %}
<main class="admin-panel">
    <h1 class="admin-titulo">🎮 Panel de Administración</h1>

    <section class="admin-bloque gestion-contenido">
        <h2>Gestión de Contenido</h2>
        <ul>
            <li>📰 Noticias: <strong>{{ noticias|length }}</strong></li>
            <li>✏️ Noticias en borrador: {{ noticias|filter(n => n.estado == 'borrador')|length }}</li>
            <li>🔎 Noticias publicadas: {{ noticias|filter(n => n.estado == 'publicado')|length }}</li>
            <li>📂 Categorías: <a href="#">Administrar categorías</a></li>
        </ul>
    </section>

    <section class="admin-bloque estadisticas">
        <h2>Estadísticas del Sitio</h2>
        <div class="filtros">
            <button>Esta Semana</button>
            <button>Este Mes</button>
            <button>Este Año</button>
        </div>
        <ul>
            <!-- Aquí podrías agregar métricas -->
        </ul>
    </section>

    <section class="admin-bloque moderacion">
        <h2>Moderación</h2>
        <ul>
            <li>
                🚨 <a href="{{ path('admin_denuncias') }}">Denuncias: {{ denunciasPendientes|length }} pendientes de revisar</a>
                {% if denunciasPendientes|length > 0 %}
                    <span class="alerta">¡Requiere atención!</span>
                {% endif %}
            </li>
            <li>
                👥 <a href="{{ path('admin_usuarios') }}">Gestión de Usuarios: {{ usuarios|length }} registrados</a>
            </li>
        </ul>
    </section>
</main>
{% endblock %}
